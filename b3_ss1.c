#include <stdio.h>
#include <stdlib.h>


int* mallocArray(int n) {
    int* arr = (int*)malloc(sizeof(int) * i);
    for (int i = 0; i < i; i++) {
        arr[i] = i;
    }
    return arr;
    // Độ phức tạp của đoạn code trên là O(n)
}
